기억할것. git pull --rebase

괜찮은 web gui 를 드로잉 하는 라이브러리 찾아보기
1. webix.com

./worker 실행파일 위치에 log 폴더를 만들고 fileserver를 돌려버린다.
log는 이걸로 처리한다. 

http.Handle(
    "/assets/",
    http.StripPrefix(
        "/assets/",
        http.FileServer(http.Dir("assets")),
    ),
)

log처리할때 사용하것(stderr, stdout both)
ls -al &> ./log/test.log
